# ui_auto_test
基于cv2和ocr的ui自动化脚本  
##安装依赖,也可以安装最新版本  
`pip install -r requirements.txt`
##1.在test目录创建版本新文件夹，也可以在run_fenbao_test.py中输入版本文件夹名称会自动创建  
##2.将分包apk都在下载到刚刚创建的文件夹内  
##3.然后执行run_fenbao_test.py
